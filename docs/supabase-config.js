// Public client configuration for article reactions.
// Supabase publishable/anon keys are safe to expose here only when RLS is enabled.
// Never place a service_role key in this file.
window.KIRAPARA_SUPABASE = Object.freeze({
  url: '',
  key: '',
});
